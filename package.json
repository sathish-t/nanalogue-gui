{
  "name": "@nanalogue/gui",
  "version": "0.1.0",
  "description": "GUI tools for nanopore data analysis",
  "main": "dist/main.js",
  "bin": {
    "nanalogue-gui": "./bin/nanalogue-gui.js"
  },
  "scripts": {
    "build": "tsc && npm run build:renderer && npm run copy-assets",
    "build:renderer": "npm run build:landing && npm run build:swipe && npm run build:qc",
    "build:landing": "esbuild src/renderer/landing/landing.ts --bundle --outfile=dist/renderer/landing/landing.js --format=iife --platform=browser",
    "build:swipe": "esbuild src/renderer/swipe/swipe.ts --bundle --outfile=dist/renderer/swipe/swipe.js --format=iife --platform=browser",
    "build:qc": "esbuild src/renderer/qc/qc-config.ts src/renderer/qc/qc-results.ts --bundle --outdir=dist/renderer/qc --format=iife --platform=browser",
    "copy-assets": "npm run copy:shared && npm run copy:landing && npm run copy:swipe && npm run copy:qc",
    "copy:shared": "mkdir -p dist/renderer/shared && cp src/renderer/shared/styles.css node_modules/chart.js/dist/chart.umd.js dist/renderer/shared/",
    "copy:landing": "mkdir -p dist/renderer/landing && cp src/renderer/landing/landing.html src/renderer/landing/landing.css dist/renderer/landing/",
    "copy:swipe": "mkdir -p dist/renderer/swipe && cp src/renderer/swipe/swipe.html dist/renderer/swipe/",
    "copy:qc": "mkdir -p dist/renderer/qc && cp src/renderer/qc/*.html src/renderer/qc/*.css dist/renderer/qc/ 2>/dev/null || true",
    "start": "npm run build && electron dist/main.js",
    "dev": "npm run build && electron dist/main.js",
    "lint": "biome check src/ && eslint src/ && stylelint \"src/**/*.css\" && html-validate \"src/**/*.html\"",
    "lint:fix": "biome check --write src/ && eslint --fix src/ && stylelint --fix \"src/**/*.css\"",
    "lint:docs": "eslint src/",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:css:fix": "stylelint --fix \"src/**/*.css\"",
    "lint:html": "html-validate \"src/**/*.html\"",
    "test": "echo 'No tests configured yet'"
  },
  "dependencies": {
    "@nanalogue/node": "^0.1.2",
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@eslint/js": "^9.39.2",
    "@stylistic/stylelint-plugin": "^5.0.1",
    "@types/node": "^22.0.0",
    "electron": "^40.1.0",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "eslint-plugin-jsdoc": "^62.5.2",
    "html-validate": "^10.7.0",
    "stylelint": "^17.1.1",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-order": "^7.0.1",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.54.0"
  },
  "engines": {
    "node": ">= 22"
  },
  "author": "Sathish Thiyagarajan",
  "license": "MIT"
}
